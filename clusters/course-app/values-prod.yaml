replicaCount: 3

image:
  repository: monah0/course-app
  tag: latest
  pullPolicy: IfNotPresent

service:
  type: ClusterIP
  port: 80

ingress:
  enabled: true
  className: nginx
  host: course-app.local

redis:
  host: dragonfly
  port: 6379

app:
  store: dragonfly
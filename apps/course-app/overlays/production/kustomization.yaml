namespace: production

resources:
  - ../../base
  - dragonfly.yaml
  - hpa.yaml

patches:
  - target:
      kind: Deployment
      name: course-app
    patch: |
      - op: replace
        path: /spec/replicas
        value: 3

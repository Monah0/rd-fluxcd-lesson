namespace: development

resources:
  - namespace.yaml
  - ../../base
  - dragonfly.yaml

patches:
  - target:
      kind: Deployment
      name: course-app
    patch: |
      - op: replace
        path: /spec/replicas
        value: 1
